<%= form_with(model: airway) do |form| %>
  <% if airway.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(airway.errors.count, "error") %> prohibited this airway from being saved:</h2>

      <ul>
        <% airway.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :max_planes %>
    <%= form.number_field :max_planes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
