<%= form_with(model: discharge) do |form| %>
  <% if discharge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(discharge.errors.count, "error") %> prohibited this discharge from being saved:</h2>

      <ul>
        <% discharge.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :liters %>
    <%= form.number_field :liters %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
