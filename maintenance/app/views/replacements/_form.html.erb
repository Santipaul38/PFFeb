<%= form_with(model: replacement) do |form| %>
  <% if replacement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(replacement.errors.count, "error") %> prohibited this replacement from being saved:</h2>

      <ul>
        <% replacement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :manufacture_date %>
    <%= form.text_field :manufacture_date %>
  </div>

  <div class="field">
    <%= form.label :serial_number %>
    <%= form.number_field :serial_number %>
  </div>

  <div class="field">
    <%= form.label :brand_id %>
    <%= form.collection_select(:brand_id, Brand.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
